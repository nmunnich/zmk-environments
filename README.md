# ZMK Environments

Contains experimental nix files defining:
- Build & validation docker images
- Build & Dev shells, for use with zmk-config and zmk-workspace respectively
- Docker file defining a basic image suitable to be loaded by vscode as a devcontainer which can load the build shell/devshell

